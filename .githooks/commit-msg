#!/usr/bin/env sh
set -euo pipefail

if ! command -v bun >/dev/null 2>&1; then
  echo "bun is required for commit message checks but was not found in PATH." >&2
  exit 1
fi

bun scripts/check-commit-msg.ts "$1"
